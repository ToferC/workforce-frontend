query VacantRoles($count: Int!) {
  vacantRoles(count: $count) {
    id
    titleEnglish
    team {
      id
      nameEnglish
    }
    requirements {
      id
      nameEn
      requiredLevel
    }
    findMatches {
      id
    }
  }
}
